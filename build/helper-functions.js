function initHelper() {
    if (process.argv.length < 1) {
        console.error('No action defined');
        return;
    }
    switch (process.argv[0]) {
        case 'new-customer':
            break;
    }
}
initHelper();
